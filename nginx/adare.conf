# ADARE GENERAL HOSPITAL — Nginx reverse proxy (production)
server {
    listen 80;
    server_name adaregeneralhospital.com;
    # After TLS is issued, redirect all HTTP to HTTPS:
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://api:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        # SSE support
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_read_timeout 3600s;
    }

    client_max_body_size 8m;
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;
}

# HTTPS block — enable after issuing certificates (certbot --nginx or DNS):
# server {
#     listen 443 ssl http2;
#     server_name adaregeneralhospital.com;
#     ssl_certificate     /etc/letsencrypt/live/adaregeneralhospital.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/adaregeneralhospital.com/privkey.pem;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     location / { proxy_pass http://api:4000; proxy_http_version 1.1; proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto https; proxy_set_header Connection ''; proxy_buffering off;
#         proxy_read_timeout 3600s; }
#     client_max_body_size 8m;
# }
